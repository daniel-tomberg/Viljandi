[phases.setup]
nixPkgs = ["openjdk17", "maven"]

[phases.install]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "cd backend && java -jar target/customer-order-portal-0.0.1-SNAPSHOT.jar"

[variables]
JAVA_HOME = "/usr/lib/jvm/java-17-openjdk"
